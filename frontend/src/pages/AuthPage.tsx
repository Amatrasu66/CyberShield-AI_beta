import { ShieldCheck } from 'lucide-react';
import { Link } from 'react-router-dom';
import { authContent } from '../data/mockData';
import { Button, TextInput } from '../components/ui';

export interface AuthPageProps { readonly mode: keyof typeof authContent; }
export function AuthPage({ mode }: AuthPageProps) { const content = authContent[mode]; const reset = mode === 'forgot'; return <main className="grid min-h-screen bg-background lg:grid-cols-2"><section className="relative hidden overflow-hidden border-r bg-surface-lowest p-12 lg:block"><div className="grid-glow absolute inset-0" /><div className="relative flex h-full flex-col justify-between"><div className="flex items-center gap-3 font-display text-xl font-bold"><span className="grid h-10 w-10 place-items-center rounded bg-primary text-primary-foreground"><ShieldCheck /></span>CyberShield AI</div><div className="max-w-md"><p className="eyebrow mb-4">Security intelligence</p><h1 className="font-display text-5xl font-bold leading-tight tracking-tight">Know your exposure. <span className="text-primary">Act with confidence.</span></h1><p className="mt-5 leading-7 text-on-surface-variant">A focused workspace for security checks, incident insights, and practical remediation.</p></div><p className="font-mono text-xs text-on-surface-variant">CYBERSHIELD AI · STATIC PRODUCT DEMO</p></div></section><section className="flex items-center justify-center p-6"><div className="w-full max-w-md"><Link to="/dashboard" className="mb-10 flex items-center gap-3 font-display text-xl font-bold lg:hidden"><span className="grid h-9 w-9 place-items-center rounded bg-primary text-primary-foreground"><ShieldCheck size={20} /></span>CyberShield AI</Link><p className="eyebrow mb-3">Secure access</p><h2 className="font-display text-3xl font-bold">{content.title}</h2><p className="mt-3 text-sm leading-6 text-on-surface-variant">{content.description}</p><form className="mt-8 grid gap-5" onSubmit={(event) => event.preventDefault()}>{!reset && mode === 'register' && <TextInput label="Full name" placeholder="Avery Sharma" />}{<TextInput label="Email address" type="email" placeholder="you@company.com" />}{!reset && <TextInput label="Password" type="password" placeholder="••••••••••••" />}<Button type="submit" className="mt-2 w-full">{content.action}</Button></form><p className="mt-6 text-center text-sm text-on-surface-variant">{content.prompt} <Link className="font-semibold text-primary hover:underline" to={content.to}>{content.link}</Link></p></div></section></main>; }
