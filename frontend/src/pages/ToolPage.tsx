import { Play, RefreshCw } from 'lucide-react';
import { PageHeader } from '../components/PageHeader';
import { Badge, Button, Card, DataTable } from '../components/ui';
import { tools } from '../data/mockData';

export interface ToolPageProps { readonly path: keyof typeof tools; }
export function ToolPage({ path }: ToolPageProps) { const tool = tools[path]; const multiline = tool.inputLabel.includes('content') || tool.inputLabel.includes('Log'); return <><PageHeader eyebrow={tool.eyebrow} title={tool.title} description={tool.description} actions={<Button variant="secondary"><RefreshCw size={16} /> History</Button>} /><div className="grid gap-5 xl:grid-cols-[1fr_0.9fr]"><Card className="p-5"><p className="font-display text-lg font-semibold">Run analysis</p><p className="mt-1 text-sm text-on-surface-variant">This interface uses static mock data only.</p><label className="mt-6 grid gap-2 text-sm font-medium"><span>{tool.inputLabel}</span>{multiline ? <textarea rows={9} className="resize-none rounded border bg-surface-low p-3 font-mono text-sm placeholder:text-on-surface-variant/60 focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/20" placeholder={tool.inputPlaceholder} /> : <input className="h-11 rounded border bg-surface-low px-3 placeholder:text-on-surface-variant/60 focus:border-primary focus:outline-none focus:ring-2 focus:ring-primary/20" placeholder={tool.inputPlaceholder} />}</label><Button className="mt-5"><Play size={16} /> {tool.action}</Button></Card><Card className="p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-sm text-on-surface-variant">{tool.resultTitle}</p><p className="mt-3 font-display text-3xl font-bold">{tool.resultValue}</p></div><Badge tone={tool.status}>{tool.status}</Badge></div><div className="mt-6 rounded border bg-surface-low p-4"><p className="eyebrow mb-2">Assessment summary</p><p className="text-sm leading-6 text-on-surface-variant">{tool.resultDetail}</p></div></Card></div><Card className="mt-5"><div className="border-b px-5 py-4"><p className="font-display font-semibold">Detailed findings</p></div><DataTable headers={tool.tableHeaders} rows={tool.rows} /></Card></>; }
